- @scraps[:paginated].each do |scrap|
  - scrap = Presenter::Scrap.new(scrap).prepare

  .row.no-margin-bottom
    #note.content.col.s12.m8.offset-m2
      %h6
        #{scrap.title}
      %span.subhead Posted: #{scrap.date}
      %p
        #{scrap.content}

      .divider
