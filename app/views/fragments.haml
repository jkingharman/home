- @frags[:paginated].each do |frag|
  - frag = Presenter::Fragment.new(frag).prepare

  .row.no-margin-bottom
    #note.content.col.s12.m8.offset-m2
      %h6
        #{frag.title}
      %span.subhead posted: #{frag.date}
      %p
        #{frag.content}

      .divider
