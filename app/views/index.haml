.row.no-margin-bottom
  #index.content.col.s12.m8.offset-m2
    %h6
      jkh

    .divider
    #notes
      %ul
        - asc_posted_at(@notes).each do |note|
          %li
            %span #{note.date}
            %a{href: "notes/#{note.slug}"} #{note.title}

    .divider
