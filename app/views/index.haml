.row.no-margin-bottom
  #index.col.s8.offset-s2
    %h6.black-text
      jkh
    %p skater, developer

    .divider

    #notes
      %ul
        - asc_order(@notes).each do |note|
          %li
            %span #{note.date}
            %a{href: "notes/#{note.slug}"} #{note.title}

    .divider
